import React, { Fragment } from "react";
import Layout from "../layout";

const Home = (props) => {
  return (
    <Fragment>
      <Layout />
      <h1>hello from home</h1>
    </Fragment>
  );
};

export default Home;
